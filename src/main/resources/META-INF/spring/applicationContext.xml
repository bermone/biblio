<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://cxf.apache.org/configuration/beans http://cxf.apache.org/schemas/configuration/cxf-beans.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.2.xsd">
    <!--
        This will automatically locate any and all property files you have
        within your classpath, provided they fall under the META-INF/spring
        directory. The located property files are parsed and their values can
        then be used within application context files in the form of
        ${propertyKey}.
    -->
    <beans:beans profile="DEV">
        <beans:bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
            <beans:property name="locations">
                <beans:list>
                    <beans:value>classpath:META-INF/properties/*.properties</beans:value>
                </beans:list>
            </beans:property>
        </beans:bean>  
	</beans:beans>
	<beans:beans>
	    <!--
	        Turn on AspectJ @Configurable support. As a result, any time you
	        instantiate an object, Spring will attempt to perform dependency
	        injection on that object. This occurs for instantiation via the "new"
	        keyword, as well as via reflection. This is possible because AspectJ
	        is used to "weave" Roo-based applications at compile time. In effect
	        this feature allows dependency injection of any object at all in your
	        system, which is a very useful feature (without @Configurable you'd
	        only be able to dependency inject objects acquired from Spring or
	        subsequently presented to a specific Spring dependency injection
	        method). Roo applications use this useful feature in a number of
	        areas, such as @PersistenceContext injection into entities.
	    -->
	    <aop:aspectj-autoproxy/>
	    <context:spring-configured/>
	    <!--
	        This declaration will cause Spring to locate every @Component,
	        @Repository and @Service in your application. In practical terms this
	        allows you to write a POJO and then simply annotate the new POJO as an
	        @Service and Spring will automatically detect, instantiate and
	        dependency inject your service at startup time. Importantly, you can
	        then also have your new service injected into any other class that
	        requires it simply by declaring a field for your service inside the
	        relying class and Spring will inject it. Note that two exclude filters
	        are declared. The first ensures that Spring doesn't spend time
	        introspecting Roo-specific ITD aspects. The second ensures Roo doesn't
	        instantiate your @Controller classes, as these should be instantiated
	        by a web tier application context. Refer to web.xml for more details
	        about the web tier application context setup services.
	        
	        Furthermore, this turns on @Autowired, @PostConstruct etc support. These 
	        annotations allow you to use common Spring and Java Enterprise Edition 
	        annotations in your classes without needing to do any special configuration. 
	        The most commonly used annotation is @Autowired, which instructs Spring to
	        dependency inject an object into your class.
	    -->
	    <context:component-scan base-package="fr.bermosa">
	        <context:exclude-filter expression=".*_Roo_.*" type="regex"/>
	        <context:exclude-filter expression="org.springframework.stereotype.Controller" type="annotation"/>
	    </context:component-scan>
	    <beans:bean class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close" id="dataSource">
	        <beans:property name="driverClassName" value="${database.driverClassName}"/>
	        <beans:property name="url" value="${database.url}"/>
	        <beans:property name="username" value="${database.username}"/>
	        <beans:property name="password" value="${database.password}"/>
	        <beans:property name="testOnBorrow" value="true"/>
	        <beans:property name="testOnReturn" value="true"/>
	        <beans:property name="testWhileIdle" value="true"/>
	        <beans:property name="timeBetweenEvictionRunsMillis" value="1800000"/>
	        <beans:property name="numTestsPerEvictionRun" value="3"/>
	        <beans:property name="minEvictableIdleTimeMillis" value="1800000"/>
	        <beans:property name="validationQuery" value="SELECT 1 FROM DUAL"/>
	    </beans:bean>
	    <beans:bean class="org.springframework.orm.jpa.JpaTransactionManager" id="transactionManager">
	        <beans:property name="entityManagerFactory" ref="entityManagerFactory"/>
	    </beans:bean>
	    <tx:annotation-driven mode="aspectj" transaction-manager="transactionManager"/>
	    <beans:bean class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean" id="entityManagerFactory">
	        <beans:property name="persistenceUnitName" value="persistenceUnit"/>
	        <beans:property name="dataSource" ref="dataSource"/>
	        <beans:property name="packagesToScan" value="${jpa.scan.packages}"/>
	        <beans:property name="jpaVendorAdapter">
                <beans:bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
            </beans:property>
            <beans:property name="jpaProperties">
                <beans:props>
                    <!-- shared cache modes: ALL, ENABLE_SELECTIVE, DISABLE_SELECTIVE, NONE -->
                    <beans:prop key="javax.persistence.sharedCache.mode">${jpa.cache.mode}</beans:prop>
                    <!-- value="create" to build a new database on each run; value="update" to modify an existing database; value="create-drop" means the same as "create" but also drops tables when Hibernate closes; value="validate" makes no changes to the database -->
                    <beans:prop key="hibernate.hbm2ddl.auto">${jpa.database.schema.DEV:off}</beans:prop>
                    <beans:prop key="hibernate.ejb.naming_strategy">org.hibernate.cfg.ImprovedNamingStrategy</beans:prop>
                    <beans:prop key="hibernate.connection.charSet">UTF-8</beans:prop>
                    <beans:prop key="hibernate.dialect">${database.dialect}</beans:prop>
                    <!-- Envers integration for audit functionalities -->
                    <beans:prop key="hibernate.auditable">${jpa.audit.enabled}</beans:prop>
                    <!-- Search/Lucene integration for advanced search -->
                    <beans:prop key="hibernate.search.autoregister_listeners">${jpa.search.enabled}</beans:prop>
                    <beans:prop key="hibernate.search.default.directory_provider">${database.search.provider}</beans:prop>
                    <beans:prop key="hibernate.search.default.indexBase">${database.search.indexBase}</beans:prop>
                </beans:props>
            </beans:property>
	    </beans:bean>
	    
	     <beans:bean class="org.dozer.spring.DozerBeanMapperFactoryBean" id="mapper">
	            <beans:property name="mappingFiles" value="classpath*:META-INF/*Mapping.xml"/>
	      </beans:bean>
	      <beans:bean class="org.springframework.transaction.interceptor.TransactionInterceptor" id="transactionInterceptor">
	      	<beans:property name="transactionManager" ref="transactionManager"/>
	        <beans:property name="transactionAttributes">
	        	<beans:props>
	             	<beans:prop key="save">PROPAGATION_REQUIRED</beans:prop>
	             </beans:props>
	        </beans:property>
	       </beans:bean>
	</beans:beans>
</beans:beans>
